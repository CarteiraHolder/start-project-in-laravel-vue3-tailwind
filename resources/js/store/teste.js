import { createStore } from 'vuex'

// Create a new store instance.
const store = createStore({
    state: {
        teste: 'Store de teste',
    },
    getters: {},
    actions: {
    },
    mutations: {
    },
    modules: {},
})

export default store